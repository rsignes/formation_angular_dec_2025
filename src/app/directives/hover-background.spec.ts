import { HoverBackground } from './hover-background';

describe('HoverBackground', () => {
  it('should create an instance', () => {
    const directive = new HoverBackground();
    expect(directive).toBeTruthy();
  });
});
